# yummy

TypeScript + React (material UI)

Python + FastAPI

Postgres

docker run -p 5432:5432 --name yummy -e POSTGRES_PASSWORD=postgres -d postgres:15

docker compose file that refers to a init script that create the postgres DB